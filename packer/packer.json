{
    "variables": {
      "version": 1.0.1,
      "access_key": "{{ env `AWS_ACCESS_KEY_ID` }}",
      "secret_key": "{{ env `AWS_SECRET_ACCESS_KEY`}}"
    },
  
    "builders": [
      {
        "type": "amazon-ebs",
        "ami_description": "Golden AMI v{{user `version`}}",
        "ami_name": "Golden AMI {{timestamp}}",
        "associate_public_ip_address": true,
        "communicator": "ssh",
        "instance_type": "t2.micro",
        "region": "us-east-1",
        "security_group_id": "sg-b8e4c2e3",
        "ssh_username": "ubuntu",
        "subnet_id": "subnet-c85f08af",
        "tags": {
          "Name": "Golden AMI v{{user `version`}}"
         },
         "source_ami": "ami-0cfee17793b08a293"
    }],
  
    "provisioners": [
      {
        "type": "ansible",
        "playbook_file": "../ansible/ansible_config.yml",
        "user": "ubuntu"
      }
    ],
    "post-processors": [
    {
      "type": "manifest",
      "strip_path": true
    }]
  }
  
